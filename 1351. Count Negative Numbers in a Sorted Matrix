class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        
        int count{0};
        
        for(auto el: grid)
            for(int i: el)
                if(i < 0)
                    count++;
        
        return count;
        
    }
};